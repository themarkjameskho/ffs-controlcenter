# 2026-03-17

- Workspace note: `SOUL.md`, `USER.md`, and `MEMORY.md` were not present in repo root at time of session start.
- Reviewed `docs/pipeline_markers.md` (pipeline markers + artifact filtering contract).
- Implemented `check tasks` command behavior:
  - `npm run check:tasks` CLI script + shared logic in `scripts/check-tasks-lib.mjs`.
  - Dev server endpoint: `POST /api/check-tasks`.
  - Chat bridge: `POST /api/chat` with text `check tasks`.
- Updated `docs/OPENCLAW_RUNBOOK.md` to reflect the implemented entrypoints and status.
- Ensured marker JSON never appears in Control Center artifact listings by excluding `.ff/` directories and any `*/.ff/*.json` paths in the deliverables indexer.
- Renamed client label for `bed_bug_bbq` from “Bed Bug To Go” to “Bed Bug BBQ”.
